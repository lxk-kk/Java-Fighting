【HTTP2】
优势：
	1、信道复用
	2、分帧传输
		数据的传输方式，以分帧传输时，每一帧都有上下文关系。即：http请求的数据并不需要连续的进行发送。当数据到达服务端，服务端会根据每一帧中的上下文信息将数据按顺序进行组合。
		这种传输方式，使得HTTP2在tcp连接中可以并发的发送请求。
	3、Server Push
		在传统的http协议中，一般是客户端主动，服务端被动，只有客户端发送了一个请求，服务端才能对客户端进行响应，服务端不能主动向客户端推送内容，server_push便是解决这个问题
	http2性能提升主要在于信道复用和分帧传输（支持并发的发送和响应）！